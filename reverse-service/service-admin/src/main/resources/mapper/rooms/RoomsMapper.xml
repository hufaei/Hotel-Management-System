<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sz.admin.rooms.mapper.RoomsMapper">
    <!-- 通用映射 -->
    <resultMap id="BaseResultMap" type="com.sz.admin.rooms.pojo.po.Rooms">
        <id column="room_id" property="roomId"/>
        <result column="hotel_id" property="hotelId"/>
        <result column="room_number" property="roomNumber"/>
        <result column="room_type" property="roomType"/>
        <result column="price" property="price"/>
        <result column="size" property="size"/>
        <result column="bed_info" property="bedInfo"/>
        <result column="capacity" property="capacity"/>
        <result column="availability" property="availability"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        hotel_id, room_number, room_type, price, size, bed_info, capacity, availability
    </sql>

</mapper>
